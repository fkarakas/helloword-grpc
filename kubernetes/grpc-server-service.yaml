apiVersion: v1
kind: Service
metadata:
  name: helloworld-grpc-server
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: helloworld-grpc-server
  ports:
  - protocol: TCP
    port: 50051
    targetPort: 50051
